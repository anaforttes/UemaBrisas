# Exemplo de configuração do banco de dados (Neon DB / PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database_name?sslmode=require

# Chave secreta para JWT
JWT_SECRET=your-secret-key-here

# Porta da aplicação
PORT=3001
